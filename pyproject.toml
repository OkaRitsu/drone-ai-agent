[project]
name = "drone-ai-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0.0",
    "opencv-python>=4.10.0",
    "rerun-sdk>=0.22.1",
]
